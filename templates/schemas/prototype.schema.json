{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "prototype input",
  "type": "object",
  "required": ["feature", "prototype_html"],
  "properties": {
    "feature": {
      "type": "object",
      "required": ["name", "slug"],
      "properties": {
        "name": {"type": "string"},
        "slug": {"type": "string"}
      }
    },
    "prototype_html": {
      "type": "string",
      "description": "Full HTML for the prototype body: inline <style>, mock data, and inline <script>. Zero external dependencies. Mobile-first 375px viewport. Will be wrapped by a minimal HTML shell."
    }
  }
}
