# {{ feature.name }} BDD 測試案例

共 {{ scenarios | length }} 個 Scenario。

## 索引
| ID | 標題 | 類型 |
|----|------|------|
{% for s in scenarios %}
| [`{{ s.id }}`](#{{ s.id }}) | {{ s.title }} | {{ s.kind }} |
{% endfor %}

---

{% for s in scenarios %}
### <a id="{{ s.id }}"></a> Scenario: {{ s.title }}
**類型**：{{ s.kind }}（正常 / 邊界 / 異常）

**Gherkin（中）**
```gherkin
# language: zh-TW
{{ s.gherkin_zh }}
```

{% if s.gherkin_en %}
**Gherkin（英）**
```gherkin
{{ s.gherkin_en }}
```
{% endif %}

**互動時序圖**
```mermaid
{{ s.sequence_diagram }}
```

**涉及 API**
{% for a in s.apis %}
- [`{{ a }}`](./{{ feature.slug }}-spec-advanced.md#{{ a }})
{% endfor %}

**後端資料來源**
{% for d in s.data_sources %}
- {{ d }}
{% endfor %}

---

{% endfor %}
