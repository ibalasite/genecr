# {{ feature.name }} SCRUM 故事卡

## Epic：{{ epic.title }}
{{ epic.desc }}

**總計**：{{ stories | length }} 張故事卡，預估 {{ stories | sum(attribute='points') }} 點。

---

{% for group in groups %}
## {{ group.icon }} {{ group.name }}

{% for story in stories if story.group == group.key %}
### Story #{{ story.id }}：{{ story.title }}
**作為** {{ story.role }}，**我想要** {{ story.want }}，**以便** {{ story.benefit }}

**驗收標準**：
{% for ac in story.acceptance %}
- [ ] {{ ac }}
{% endfor %}

- **Story Points**：{{ story.points }}
- **負責團隊**：{% for t in story.teams %}`{{ t }}`{% if not loop.last %} · {% endif %}{% endfor %}
{% if story.depends_on %}
- **依賴**：{% for d in story.depends_on %}#{{ d }}{% if not loop.last %}, {% endif %}{% endfor %}
{% endif %}
{% if story.assets %}
- **資源**：{% for a in story.assets %}[`{{ a }}`](./{{ feature.slug }}-assets.md#{{ a }}){% if not loop.last %}, {% endif %}{% endfor %}
{% endif %}
{% if story.tech_notes %}
- **技術備注**：{{ story.tech_notes }}
{% endif %}

---

{% endfor %}
{% endfor %}
